docker run -it ubuntu:latest
apt update
sudo apt install software-properties-common
sudo add-apt-repository ppa:deadsnakes/ppa
sudo apt install build-essential zlib1g-dev libncurses5-dev libgdbm-dev libnss3-dev libssl-dev libreadline-dev libffi-dev wget
sudo apt install ansible
apt --fix-broken install

copy file to another server cmd:
scp ./temp.pub  nadir@172.26.242.176:/tmp

CHECK ANSIBLE ONCE INSTALLED
# ansible --version

  config file = /etc/ansible/ansible.cfg
  configured module search path = ['/root/.ansible/plugins/modules', '/usr/share/ansible/plugins/modules']
  ansible python module location = /usr/lib/python3/dist-packages/ansible
  ansible collection location = /root/.ansible/collections:/usr/share/ansible/collections
  executable location = /usr/bin/ansible
  python version = 3.10.12 (main, Jun 11 2023, 05:26:28) [GCC 11.4.0] (/usr/bin/python3)
  jinja version = 3.0.3
  libyaml = True
root@ansible-master:/home/nadir#


RE-INSTALL ANSIBLE

sudo apt remove ansible
sudo apt update
sudo apt install software-properties-common
sudo apt-add-repository --yes --update ppa:ansible/ansible
sudo apt install ansible


create Image from Modified Container
1- stop container
2- :> docker commit (type containter name)




   41  sudo apt update
   42  apt instlal ansible
   43  apt install ansible
   44  cd ..
   45  which ansible
   46  ping yahoo.com
   47  apt install ansible
   48  l
   49  which ansible
   50  cd etc
   51  ls
   52  cd ansible/
   53  ls
   54  ls -l
   55  cat hosts
   56  apt install pythone
   57  apt install pyton
   58  apt install pyton:latest
   59  apt install pyton3
   60  sudo apt install pyton3.8
   61  pythone --version
   62  sudo apt install build-essential zlib1g-dev libncurses5-dev libgdbm-dev libnss3-dev libssl-dev libreadline-dev libffi-dev wget
   63  ls
   64  which ansible
   65  cd ..
   66  ls
   67  history
